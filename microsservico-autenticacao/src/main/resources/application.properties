server.port=8082
spring.application.name=microsservico-autenticacao

# A porta TCP que o servidor gRPC ficará escutando na rede
grpc.server.port=8083

# URL do Eureka Server do grupo
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.enabled=false
